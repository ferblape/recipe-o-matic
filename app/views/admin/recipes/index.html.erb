<div class="pure-u-3-4">
  <div class="pure-g">
    <div class="pure-u-1">
      <ul>
      <% @recipes.each do |recipe| %>
        <li><%= link_to(recipe.name, edit_admin_recipe_path(recipe)) %></li>
      <% end %>
      </ul>
    </div>
  </div>
</div>
