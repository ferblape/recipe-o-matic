<dl class="lists">
  <% @lists.each do |list| %>
    <dt><%= link_to(list.name, list_path(list)) %></dd>
    <dd>
      <%= t('.recipes', count: list.recipes.count) %> · <%= t('.ingredients', count: list.ingredients.size) %>
    </dd>
  <% end %>
</dl>
