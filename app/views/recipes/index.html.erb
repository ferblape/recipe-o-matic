<% @recipes.each do |recipe| %>
  <div class="recipe-entry">
    <% if recipe.images.any? %>
      <%= link_to(image_tag(recipe.images.first, alt: recipe.name), recipe_path(recipe), title: recipe.name) %>
    <% end %>
    <h2><%= link_to(recipe.name, recipe_path(recipe), title: recipe.name) %></h2>
    <p>
      <span class="date"><%= l(recipe.created_at, format: :short) %></span>
      —
      <%= recipe.ingredients.limit(5).map(&:food).map(&:name).join(', ') %></p>
    </p>
  </div>
<% end %>
